backend:
  name: git-gateway
  branch: admin #
media_folder: "assets/images/posts"

collections:
  - name: "shorts" 
    label: "Shorts" # Used in the UI
    folder: "short/_posts" # The path to the folder where the documents are stored
    create: true 
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields: 
      - {label: "Layout", name: "layout", widget: "hidden", default: "post"}
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Excerpt", name: "excerpt_separator", widget: "select", options: ["", "<!--more-->"]}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Link URL", name: "link_url", widget: "string"}
      - {label: "Short URL", name: "short_url", widget: "string"}
      - {label: "Body", name: "body", widget: "markdown"}
